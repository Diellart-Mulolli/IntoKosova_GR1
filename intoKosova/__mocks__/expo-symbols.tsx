import React from "react";
import { View } from "react-native";

// Mock i thjeshtë për component-in
export const SymbolView = (_props: any) => {
  return <View />;
};

// Mock për types / enums që përdoren në IconSymbol
export const SymbolWeight = {};
export type SymbolViewProps = any;
